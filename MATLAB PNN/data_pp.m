filename = 'Data.csv';
Data = readmatrix(filename);
Data1 = Data(1:168,1:3);
y = Data(1:168,4);
D = array2table(Data1, 'VariableNames', {'Del_V','V','Del_T'});
D1 = array2table(y, 'VariableNames', {'SOH'});
x = normalize(D);
xy = [x D1];
[m,n] = size(xy) ;
P = 0.80 ;
shuf = randperm(m)  ;
x_train = xy(shuf(1:round(P*m)),1:3) ; 
y_train = xy(shuf(1:round(P*m)),4) ; 
x_test = xy(shuf(round(P*m)+1:end),1:3) ;
y_test = xy(shuf(round(P*m)+1:end),4) ;
data.x_train_f = x_train;
data.x_test_f = x_test;
data.y_train_f = y_train;
data.y_test_f = y_test;
%net = newpnn(P,T,spread)
%keySet = {'x_train','x_test'}; %,'y_train','y_test'};
%valueSet = [[x_train] [x_test]];
%M = containers.Map(keySet,valueSet)
%data = py.dict(pyargs('x_train',x_train, 'x_test', x_test, 'y_train', y_train, 'y_test', y_test));